--building destruction mechanic

local building = script.parent

local.fuction = collapse
local.function = collapse()
local building("model")
  if ("damage") then part.anchored = false
  end
end

local vfx = script.parent
local.function = (vfx.id) = "id///("id number for vfx")
play.vfx = ("id number")

end

